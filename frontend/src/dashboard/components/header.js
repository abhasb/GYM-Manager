import logo from '../../../src/gym.png'

const CustomHeader = () => {
    return (
        <div style={{
            paddingLeft: 35,
            fontSize: 30,
            fontWeight: 800,
            color: "#FFFFFF",
            fontFamily: "revert",
            textAlign: "center",
            display: "flex",
            alignItems: "center"
        }}>
            <p style={{paddingTop: 13}}>GYM WALA</p>
        </div>
    );
}

export default CustomHeader;